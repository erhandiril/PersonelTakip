
@{
    ViewBag.Title = "RecoveryPassword";
}

<div class="row">
    <div class="col-md-offset-3 col-md-6">
        @using (Html.BeginForm("RecoveryPassword", "Account", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            @Html.TextBox("email", null, new { @class = "form-control", placeholder = "E-mail adresi" })<br />
            <input type="submit" value="Şifremi gönder btn btn-warning" />
        }
        <div class="text-center text-info">
            <h1>@Html.Raw(ViewBag.sonuc)</h1>
        </div>
    </div>
</div>

